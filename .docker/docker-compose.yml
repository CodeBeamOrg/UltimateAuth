services:
  ultimateauth:
    build:
      context: ..
      dockerfile: .docker/Dockerfile
    container_name: ultimateauth
    restart: always
    networks:
      - edge

networks:
  edge:
    external: true
